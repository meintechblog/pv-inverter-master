---
phase: 36-auto-throttle-ui-live-tuning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pv_inverter_proxy/config.py
  - src/pv_inverter_proxy/distributor.py
  - src/pv_inverter_proxy/webapp.py
  - tests/test_distributor.py
  - tests/test_webapp.py
autonomous: true
requirements: [THRT-10, THRT-12]

must_haves:
  truths:
    - "Virtual snapshot API returns enriched contributions with throttle_score, throttle_mode, throttle_state, measured_response_time_s, relay_on per device"
    - "Config POST accepts auto_throttle_preset field and persists it"
    - "Distributor reads convergence parameters from preset config instead of module-level constants"
    - "Virtual snapshot WS broadcast includes auto_throttle_preset"
  artifacts:
    - path: "src/pv_inverter_proxy/config.py"
      provides: "AUTO_THROTTLE_PRESETS dict and auto_throttle_preset field on Config"
      contains: "AUTO_THROTTLE_PRESETS"
    - path: "src/pv_inverter_proxy/distributor.py"
      provides: "Config-driven convergence parameters via _get_convergence_params()"
      contains: "_get_convergence_params"
    - path: "src/pv_inverter_proxy/webapp.py"
      provides: "Enriched contributions payload with throttle metadata"
      contains: "throttle_state"
  key_links:
    - from: "src/pv_inverter_proxy/distributor.py"
      to: "src/pv_inverter_proxy/config.py"
      via: "AUTO_THROTTLE_PRESETS lookup in _get_convergence_params()"
      pattern: "AUTO_THROTTLE_PRESETS"
    - from: "src/pv_inverter_proxy/webapp.py"
      to: "src/pv_inverter_proxy/distributor.py"
      via: "_device_states access for throttle_state derivation"
      pattern: "distributor._device_states"
---

<objective>
Enrich the backend APIs with throttle metadata and preset configuration so the frontend can display live throttle state, scores, and preset controls.

Purpose: The frontend (Plan 02) needs enriched contribution payloads and preset config support. This plan adds all backend data plumbing.
Output: Enriched virtual snapshot API, preset config field, config-driven distributor parameters.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-auto-throttle-ui-live-tuning/36-RESEARCH.md
@src/pv_inverter_proxy/config.py
@src/pv_inverter_proxy/distributor.py
@src/pv_inverter_proxy/webapp.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preset config, config-driven distributor params, and enriched contributions</name>
  <files>
    src/pv_inverter_proxy/config.py,
    src/pv_inverter_proxy/distributor.py,
    src/pv_inverter_proxy/webapp.py,
    tests/test_distributor.py,
    tests/test_webapp.py
  </files>
  <read_first>
    src/pv_inverter_proxy/config.py,
    src/pv_inverter_proxy/distributor.py,
    src/pv_inverter_proxy/webapp.py,
    tests/test_distributor.py,
    tests/test_webapp.py
  </read_first>
  <behavior>
    - Test: AUTO_THROTTLE_PRESETS has keys "aggressive", "balanced", "conservative" each with convergence_tolerance_pct, convergence_max_samples, target_change_tolerance_pct, binary_off_threshold_w
    - Test: Config().auto_throttle_preset defaults to "balanced"
    - Test: distributor._get_convergence_params() returns balanced values by default
    - Test: distributor._get_convergence_params() returns aggressive values when config.auto_throttle_preset = "aggressive"
    - Test: _build_virtual_contributions() includes throttle_score, throttle_mode, measured_response_time_s, throttle_state, relay_on in each contribution dict
    - Test: virtual_snapshot_handler response includes auto_throttle_preset
    - Test: config POST with auto_throttle_preset="aggressive" persists it
  </behavior>
  <action>
    **config.py changes (per THRT-12):**
    1. Add `AUTO_THROTTLE_PRESETS` dict at module level (after imports) with three presets:
       - "aggressive": convergence_tolerance_pct=10.0, convergence_max_samples=5, target_change_tolerance_pct=5.0, binary_off_threshold_w=100.0
       - "balanced": convergence_tolerance_pct=5.0, convergence_max_samples=10, target_change_tolerance_pct=2.0, binary_off_threshold_w=50.0
       - "conservative": convergence_tolerance_pct=3.0, convergence_max_samples=20, target_change_tolerance_pct=1.0, binary_off_threshold_w=25.0
    2. Add `auto_throttle_preset: str = "balanced"` field to Config dataclass (after `auto_throttle: bool = False`)
    3. In `_build_config_dataclass()` (or wherever Config fields are loaded from YAML dict), handle `auto_throttle_preset`

    **distributor.py changes (per THRT-12):**
    1. Add method `_get_convergence_params(self) -> dict` on PowerLimitDistributor that reads `self._config.auto_throttle_preset` and returns the matching preset from AUTO_THROTTLE_PRESETS (default to "balanced")
    2. Replace all references to module-level constants CONVERGENCE_TOLERANCE_PCT, CONVERGENCE_MAX_SAMPLES, CONVERGENCE_NEAR_ZERO_W (keep as CONVERGENCE_NEAR_ZERO_W since it's not preset-tunable), TARGET_CHANGE_TOLERANCE_PCT with calls to `self._get_convergence_params()`. Specifically in _update_convergence_tracking and wherever these constants are used.
    3. Keep the module-level constants as fallback documentation but use config values at runtime.

    **webapp.py changes (per THRT-10):**
    1. In `_build_virtual_contributions()`, after existing contribution dict construction, add throttle enrichment:
       - Get `throttle_score` and `throttle_mode` from plugin.throttle_capabilities (with hasattr guard)
       - Get `measured_response_time_s` and `relay_on` from distributor._device_states[inv.id]
       - Derive `throttle_state` string: "disabled" (throttle_enabled=False), "startup" (distributor._is_in_startup), "cooldown" (binary + within cooldown_s), "throttled" (limit<100 or relay off), "active" (else)
       - Add all five fields to contribution dict
    2. In `broadcast_virtual_snapshot()`, add `auto_throttle_preset` to payload alongside existing `auto_throttle`
    3. In `virtual_snapshot_handler()`, add `auto_throttle_preset` to response
    4. In config save handler (around line 404-406), add: `if "auto_throttle_preset" in body: config.auto_throttle_preset = body["auto_throttle_preset"]` -- validate it is one of "aggressive", "balanced", "conservative"

    **Anti-patterns to avoid:**
    - Do NOT create new API endpoints for preset save -- reuse existing POST /api/config
    - Do NOT cache throttle_state -- derive it fresh each broadcast cycle from DeviceLimitState
    - Do NOT import time at module level in webapp.py if not already imported (check first)
  </action>
  <verify>
    <automated>cd /Users/hulki/codex/pv-inverter-proxy && python -m pytest tests/test_distributor.py tests/test_webapp.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "AUTO_THROTTLE_PRESETS" src/pv_inverter_proxy/config.py
    - grep -q "auto_throttle_preset" src/pv_inverter_proxy/config.py
    - grep -q "_get_convergence_params" src/pv_inverter_proxy/distributor.py
    - grep -q "throttle_state" src/pv_inverter_proxy/webapp.py
    - grep -q "throttle_score" src/pv_inverter_proxy/webapp.py (in _build_virtual_contributions)
    - grep -q "auto_throttle_preset" src/pv_inverter_proxy/webapp.py
    - python -m pytest tests/test_distributor.py tests/test_webapp.py -x passes
  </acceptance_criteria>
  <done>
    - AUTO_THROTTLE_PRESETS dict exists with 3 presets, each having 4 parameter keys
    - Config.auto_throttle_preset defaults to "balanced"
    - Distributor uses config-driven params instead of module-level constants
    - Each contribution dict has throttle_score, throttle_mode, measured_response_time_s, throttle_state, relay_on
    - Virtual snapshot API and WS broadcast include auto_throttle_preset
    - Config POST accepts and validates auto_throttle_preset
    - All tests pass
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x` -- full test suite green
- `grep -c "throttle_state" src/pv_inverter_proxy/webapp.py` returns >= 1
- `grep -c "AUTO_THROTTLE_PRESETS" src/pv_inverter_proxy/config.py` returns >= 1
</verification>

<success_criteria>
Backend APIs return all throttle metadata needed by the frontend. Presets are config-driven and affect distributor behavior at runtime.
</success_criteria>

<output>
After completion, create `.planning/phases/36-auto-throttle-ui-live-tuning/36-01-SUMMARY.md`
</output>
