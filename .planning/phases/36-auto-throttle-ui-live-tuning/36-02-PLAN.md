---
phase: 36-auto-throttle-ui-live-tuning
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - src/pv_inverter_proxy/static/app.js
  - src/pv_inverter_proxy/static/style.css
autonomous: false
requirements: [THRT-10, THRT-11, THRT-12]

must_haves:
  truths:
    - "Virtual Fronius dashboard has an Auto-Throttle toggle that enables/disables the smart algorithm"
    - "Virtual dashboard shows preset buttons (Aggressive/Balanced/Conservative) that configure algorithm parameters"
    - "Contribution bar segments are colored by throttle state (active=green, throttled=orange, disabled=grey, cooldown=blue)"
    - "Throttle Overview table shows Score, Mode, Response Time, Limit, and State columns per device"
    - "Each device connection card shows throttle_score, mode, and measured response time"
    - "Auto-throttle toggle syncs with WS virtual_snapshot without flickering"
  artifacts:
    - path: "src/pv_inverter_proxy/static/app.js"
      provides: "Auto-throttle toggle, preset buttons, enhanced throttle table, throttle-state contribution bar, per-device throttle info"
      contains: "ve-auto-throttle"
    - path: "src/pv_inverter_proxy/static/style.css"
      provides: "Styles for auto-throttle card, preset group, throttle info grid"
      contains: "ve-auto-throttle-card"
  key_links:
    - from: "src/pv_inverter_proxy/static/app.js"
      to: "/api/config"
      via: "POST with auto_throttle and auto_throttle_preset"
      pattern: "auto_throttle_preset"
    - from: "src/pv_inverter_proxy/static/app.js"
      to: "WS virtual_snapshot"
      via: "updateVirtualPVPage reads auto_throttle, auto_throttle_preset, and throttle_state per contribution"
      pattern: "throttle_state"
---

<objective>
Build all frontend UI components for the Auto-Throttle feature: toggle switch, preset buttons, enhanced throttle table, state-colored contribution bar, and per-device throttle info cards.

Purpose: Users need visual controls and feedback for the auto-throttle system implemented in Phases 33-35.
Output: Complete auto-throttle UI in the virtual Fronius dashboard and per-device throttle info in individual device dashboards.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-auto-throttle-ui-live-tuning/36-RESEARCH.md
@.planning/phases/36-auto-throttle-ui-live-tuning/36-01-SUMMARY.md
@src/pv_inverter_proxy/static/app.js
@src/pv_inverter_proxy/static/style.css

<interfaces>
<!-- Plan 01 enriches the virtual snapshot API with these fields per contribution: -->
contribution dict keys (from _build_virtual_contributions):
  - device_id: str
  - name: str
  - power_w: float
  - throttle_order: int
  - throttle_enabled: bool
  - current_limit_pct: float
  - throttle_score: float (0-10)
  - throttle_mode: "proportional" | "binary" | "none"
  - measured_response_time_s: float | null
  - throttle_state: "active" | "throttled" | "disabled" | "cooldown" | "startup"
  - relay_on: bool

Virtual snapshot payload (WS + REST):
  - total_power_w: float
  - total_rated_w: float
  - virtual_name: str
  - contributions: list[dict]
  - auto_throttle: bool
  - auto_throttle_preset: "aggressive" | "balanced" | "conservative"
  - control: {clamp_min_pct, clamp_max_pct}  (REST only)

Device snapshot (from /api/devices/{id}/snapshot):
  - throttle_mode: str
  - throttle_score: float
  - measured_response_time_s: float | null (from device list API, line 881-882 in webapp.py)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auto-Throttle control card and enhanced throttle table in virtual dashboard</name>
  <files>src/pv_inverter_proxy/static/app.js, src/pv_inverter_proxy/static/style.css</files>
  <read_first>
    src/pv_inverter_proxy/static/app.js (lines 1750-1960 for buildVirtualPVPage and updateVirtualPVPage),
    src/pv_inverter_proxy/static/style.css (lines 2288-2308 for throttle table styles),
    CLAUDE.md (Venus OS design system)
  </read_first>
  <action>
    **app.js -- buildVirtualPVPage() changes (per THRT-10, THRT-12):**

    1. After the gauge card (line ~1824), add Auto-Throttle control card:
       - Create `ve-card` with inner `ve-auto-throttle-card` div
       - Left side: `ve-toggle-label` with checkbox input class `ve-auto-throttle-toggle`, checked if `data.auto_throttle`
         Uses existing ve-switch + ve-switch-knob pattern (same as config form toggles)
       - Right side: `ve-preset-group` div with three `ve-btn ve-btn--sm` buttons for "Aggressive", "Balanced", "Conservative"
         Active preset gets `ve-btn--primary` class, based on `data.auto_throttle_preset`
       - Toggle change handler: POST to `/api/config` with `{auto_throttle: toggle.checked}`, show toast on success
       - Preset click handler: POST to `/api/config` with `{auto_throttle_preset: name}`, show toast, update button classes optimistically (set clicked to `ve-btn--primary`, remove from siblings)

    2. Replace existing contribution bar color logic (line ~1861):
       - Define `THROTTLE_STATE_COLORS` map: active='var(--ve-green)', throttled='var(--ve-orange)', disabled='var(--ve-text-dim)', cooldown='var(--ve-blue)', startup='var(--ve-blue-light)'
       - Use `THROTTLE_STATE_COLORS[c.throttle_state]` for segment background instead of cycling CONTRIBUTION_COLORS
       - Update legend dot color to match

    3. Replace existing throttle table (lines 1884-1907) with enhanced 6-column table:
       - Columns: Name | Score | Mode | Response | Limit | State
       - Score: `ct.throttle_score.toFixed(1)` with `ve-mono` class, or '--'
       - Mode: `ct.throttle_mode` or '--'
       - Response: `ct.measured_response_time_s.toFixed(1) + 's'` with `ve-mono` class, or '--'
       - Limit: `ct.current_limit_pct.toFixed(1) + '%'` with `ve-mono` class, or '--'
       - State: `ve-dot` span with inline background from THROTTLE_STATE_COLORS[ct.throttle_state]

    **app.js -- updateVirtualPVPage() changes:**

    4. Add auto-throttle toggle sync (CRITICAL: avoid flicker pitfall):
       - Find `.ve-auto-throttle-toggle` checkbox in el
       - ONLY update `checked` if value differs: `if (toggle.checked !== data.auto_throttle) toggle.checked = data.auto_throttle;`
       - Update preset buttons: find all `.ve-preset-group .ve-btn` buttons, add/remove `ve-btn--primary` based on `data.auto_throttle_preset`

    5. Update contribution bar segments with new throttle state colors:
       - For each segment[i], set `background = THROTTLE_STATE_COLORS[contributions[i].throttle_state]`
       - Also update legend dot colors

    6. Update enhanced throttle table cells (now 6 columns instead of 4):
       - cells[1]: score, cells[2]: mode, cells[3]: response time, cells[4]: limit, cells[5]: state dot color

    7. Handle contribution count change: if segments.length !== contributions.length, rebuild the entire bar+legend+table section (clear card innards and reconstruct). This prevents the mismatch pitfall from research.

    **style.css additions:**

    8. Add styles:
       ```
       .ve-auto-throttle-card -- flexbox row, align-items center, justify-content space-between, gap 16px, flex-wrap wrap
       .ve-preset-group -- flexbox row, gap 8px
       .ve-throttle-info-grid -- CSS grid, 2 columns (auto 1fr), gap 4px 12px, font-size 0.85rem
       .ve-throttle-state-dot -- inline-block, 10px x 10px, border-radius 50%, vertical-align middle
       ```
       All colors use var(--ve-*) tokens. Spacing uses scale from CLAUDE.md.

    **Anti-patterns to avoid:**
    - Do NOT set toggle.checked on every WS message -- guard with if-check to prevent flicker
    - Do NOT use hardcoded hex colors -- always var(--ve-*)
    - Do NOT create new API endpoints -- POST to /api/config
    - Do NOT rebuild entire page on WS update -- use incremental DOM updates
    - Define THROTTLE_STATE_COLORS at module level (near CONTRIBUTION_COLORS) so both build and update functions can reference it
  </action>
  <verify>
    <automated>cd /Users/hulki/codex/pv-inverter-proxy && grep -c "ve-auto-throttle" src/pv_inverter_proxy/static/app.js && grep -c "THROTTLE_STATE_COLORS" src/pv_inverter_proxy/static/app.js && grep -c "ve-auto-throttle-card" src/pv_inverter_proxy/static/style.css && grep -c "auto_throttle_preset" src/pv_inverter_proxy/static/app.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "THROTTLE_STATE_COLORS" src/pv_inverter_proxy/static/app.js
    - grep -q "ve-auto-throttle-toggle" src/pv_inverter_proxy/static/app.js
    - grep -q "ve-preset-group" src/pv_inverter_proxy/static/app.js
    - grep -q "auto_throttle_preset" src/pv_inverter_proxy/static/app.js
    - grep -q "throttle_score" src/pv_inverter_proxy/static/app.js (in throttle table rows)
    - grep -q "measured_response_time_s" src/pv_inverter_proxy/static/app.js
    - grep -q "ve-auto-throttle-card" src/pv_inverter_proxy/static/style.css
    - grep -q "ve-preset-group" src/pv_inverter_proxy/static/style.css
    - grep -q "ve-throttle-info-grid" src/pv_inverter_proxy/static/style.css
    - The toggle change handler POSTs auto_throttle to /api/config
    - The preset click handler POSTs auto_throttle_preset to /api/config
    - updateVirtualPVPage guards toggle.checked with !== comparison
  </acceptance_criteria>
  <done>
    - Auto-throttle toggle renders in virtual dashboard with correct checked state
    - Three preset buttons render, active one highlighted with ve-btn--primary
    - Contribution bar segments colored by throttle state, not cycling colors
    - Throttle table has 6 columns: Name, Score, Mode, Response, Limit, State
    - WS updates sync toggle state without flicker and update all new fields
    - All styles use ve-* design tokens
  </done>
</task>

<task type="auto">
  <name>Task 2: Per-device throttle info card in individual device dashboards</name>
  <files>src/pv_inverter_proxy/static/app.js, src/pv_inverter_proxy/static/style.css</files>
  <read_first>
    src/pv_inverter_proxy/static/app.js (lines 440-520 for buildInverterDashboard),
    src/pv_inverter_proxy/static/style.css
  </read_first>
  <action>
    **app.js -- buildInverterDashboard() changes (per THRT-11):**

    1. After the gauge card section (around line ~520, after the power limit dropdowns wiring), add a throttle info card:
       - Condition: `if (data.throttle_mode && data.throttle_mode !== 'none')`
       - Create a `ve-card` with title "Throttle Info" (`ve-card-title`)
       - Inner `ve-throttle-info-grid` div (2-column CSS grid) with rows:
         - "Score" label (ve-text-dim) | value in ve-mono `(data.throttle_score || 0).toFixed(1)`
         - "Mode" label (ve-text-dim) | value `data.throttle_mode` (capitalize first letter)
         - "Response" label (ve-text-dim) | value in ve-mono: `data.measured_response_time_s != null ? data.measured_response_time_s.toFixed(1) + 's' : 'Measuring...'`
       - Append card to the topRow div (same row as gauge + AC/DC cards)

    2. The device snapshot API already returns `throttle_mode`, `throttle_score`, and `measured_response_time_s` (webapp.py lines 1558-1559 and 881-882). No backend changes needed.

    **Note:** The `ve-throttle-info-grid` style was already added in Task 1. If not, add it here:
    `.ve-throttle-info-grid { display: grid; grid-template-columns: auto 1fr; gap: 4px 12px; font-size: 0.85rem; }`
  </action>
  <verify>
    <automated>cd /Users/hulki/codex/pv-inverter-proxy && grep -c "ve-throttle-info-grid" src/pv_inverter_proxy/static/app.js && grep -c "throttle_score" src/pv_inverter_proxy/static/app.js && grep -c "Throttle Info" src/pv_inverter_proxy/static/app.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Throttle Info" src/pv_inverter_proxy/static/app.js
    - grep -q "ve-throttle-info-grid" src/pv_inverter_proxy/static/app.js (in buildInverterDashboard context)
    - grep -q "measured_response_time_s" src/pv_inverter_proxy/static/app.js (in buildInverterDashboard context)
    - The throttle info card only renders when throttle_mode is not "none"
    - Score displays with 1 decimal place
    - Response time shows "Measuring..." when null
  </acceptance_criteria>
  <done>
    - Each device dashboard with throttle_mode != "none" shows a Throttle Info card
    - Card displays Score (0-10), Mode (proportional/binary), and Response time (seconds or "Measuring...")
    - Card uses ve-throttle-info-grid layout and ve-* design tokens
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of Auto-Throttle UI</name>
  <files>src/pv_inverter_proxy/static/app.js, src/pv_inverter_proxy/static/style.css</files>
  <action>Deploy to 192.168.3.191 and verify all Auto-Throttle UI elements visually in the browser.</action>
  <what-built>
    Complete Auto-Throttle UI on the virtual Fronius dashboard and per-device throttle info cards:
    1. Auto-Throttle toggle switch (on/off) in virtual dashboard
    2. Preset buttons (Aggressive/Balanced/Conservative) with active highlight
    3. Contribution bar colored by throttle state (green/orange/grey/blue)
    4. Enhanced throttle table with Score, Mode, Response, Limit, State columns
    5. Per-device Throttle Info card on individual device dashboards
  </what-built>
  <how-to-verify>
    1. Open http://192.168.3.191:8080/#dashboard in browser
    2. Click on the Virtual PV entry in the sidebar
    3. Verify: Auto-Throttle toggle is visible below the gauge card
    4. Toggle Auto-Throttle on -- toast should confirm "Auto-Throttle enabled"
    5. Verify: Three preset buttons visible (Aggressive/Balanced/Conservative), "Balanced" highlighted by default
    6. Click "Aggressive" -- toast should confirm, button highlight changes
    7. Verify: Contribution bar segments use state colors (green for active devices)
    8. Verify: Throttle Overview table has 6 columns: Name, Score, Mode, Response, Limit, State
    9. Click on an individual inverter device in the sidebar
    10. Verify: If device has throttle capabilities, a "Throttle Info" card shows Score, Mode, and Response time
  </how-to-verify>
  <verify>
    <automated>cd /Users/hulki/codex/pv-inverter-proxy && python -m pytest tests/ -x 2>&1 | tail -5</automated>
  </verify>
  <done>User confirms all Auto-Throttle UI elements render correctly and interactions work as expected</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Virtual dashboard shows auto-throttle toggle, preset buttons, state-colored contribution bar, and enhanced throttle table
- Individual device dashboards show throttle info card when applicable
- All UI updates sync via WebSocket without page reload
- No hardcoded hex colors -- all use var(--ve-*) tokens
- `python -m pytest tests/ -x` passes
</verification>

<success_criteria>
Users can enable Auto-Throttle from the virtual dashboard, select a preset, see live throttle state per device in both the contribution bar and throttle table, and view throttle score/mode/response time on individual device dashboards.
</success_criteria>

<output>
After completion, create `.planning/phases/36-auto-throttle-ui-live-tuning/36-02-SUMMARY.md`
</output>
