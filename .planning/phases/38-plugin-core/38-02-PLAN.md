---
phase: 38-plugin-core
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - src/pv_inverter_proxy/plugins/__init__.py
autonomous: true
requirements:
  - PLUG-01
  - PLUG-02
  - PLUG-03
  - PLUG-04

must_haves:
  truths:
    - "plugin_factory creates a SungrowPlugin when entry.type == 'sungrow'"
    - "SungrowPlugin receives host, port, unit_id, and rated_power from the InverterEntry"
    - "The full test suite passes including the factory test"
  artifacts:
    - path: "src/pv_inverter_proxy/plugins/__init__.py"
      provides: "Plugin factory with sungrow branch"
      contains: "entry.type == \"sungrow\""
  key_links:
    - from: "src/pv_inverter_proxy/plugins/__init__.py"
      to: "src/pv_inverter_proxy/plugins/sungrow.py"
      via: "lazy import in factory"
      pattern: "from pv_inverter_proxy.plugins.sungrow import SungrowPlugin"
---

<objective>
Wire SungrowPlugin into the plugin factory so that config entries with type="sungrow" create SungrowPlugin instances. This completes the backend integration per the PLUGIN-CHECKLIST.md "Plugin factory" item.

Purpose: Without factory wiring, no sungrow device config entry would ever instantiate the plugin. This is the final step to make the plugin loadable at runtime.

Output: Updated `src/pv_inverter_proxy/plugins/__init__.py` with sungrow branch
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-plugin-core/38-RESEARCH.md
@.planning/phases/38-plugin-core/38-01-SUMMARY.md

@src/pv_inverter_proxy/plugins/__init__.py
@src/pv_inverter_proxy/plugins/sungrow.py
@src/pv_inverter_proxy/config.py
@docs/PLUGIN-CHECKLIST.md

<interfaces>
<!-- Current plugin factory signature -->
From src/pv_inverter_proxy/plugins/__init__.py:
```python
def plugin_factory(
    entry: InverterEntry,
    gateway_config: GatewayConfig | None = None,
):
    # existing branches: solaredge, opendtu, shelly
    # raises ValueError for unknown types
```

From src/pv_inverter_proxy/config.py:
```python
@dataclass
class InverterEntry:
    host: str = "192.168.3.18"
    port: int = 1502
    unit_id: int = 1
    type: str = "solaredge"
    rated_power: int = 0
    # ...
```

From src/pv_inverter_proxy/plugins/sungrow.py (created in Plan 01):
```python
class SungrowPlugin(InverterPlugin):
    def __init__(self, host: str, port: int, unit_id: int, rated_power: int): ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sungrow branch to plugin_factory and update error message</name>
  <files>src/pv_inverter_proxy/plugins/__init__.py</files>
  <read_first>
    - src/pv_inverter_proxy/plugins/__init__.py (current factory code)
    - src/pv_inverter_proxy/plugins/sungrow.py (SungrowPlugin constructor signature)
    - src/pv_inverter_proxy/config.py (InverterEntry fields)
    - docs/PLUGIN-CHECKLIST.md (factory checklist item)
  </read_first>
  <action>
    Edit `src/pv_inverter_proxy/plugins/__init__.py` to add a sungrow branch:

    1. After the `elif entry.type == "shelly":` block (around line 40-47), before the `else:` block, add:
    ```python
    elif entry.type == "sungrow":
        from pv_inverter_proxy.plugins.sungrow import SungrowPlugin
        return SungrowPlugin(
            host=entry.host,
            port=entry.port,
            unit_id=entry.unit_id,
            rated_power=entry.rated_power,
        )
    ```

    2. Update the ValueError message in the `else` block from:
    `f"Unknown inverter type: {entry.type} (valid: solaredge, opendtu, shelly)"`
    to:
    `f"Unknown inverter type: {entry.type} (valid: solaredge, opendtu, shelly, sungrow)"`

    3. Update the module docstring to mention sungrow as a supported type.
  </action>
  <verify>
    <automated>cd /Users/hulki/codex/pv-inverter-proxy && python -c "from pv_inverter_proxy.config import InverterEntry; from pv_inverter_proxy.plugins import plugin_factory; e = InverterEntry(type='sungrow', host='192.168.2.151', port=502, unit_id=1, rated_power=8000); p = plugin_factory(e); print(type(p).__name__, p.host, p.port, p.unit_id)" && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/pv_inverter_proxy/plugins/__init__.py contains `entry.type == "sungrow"`
    - src/pv_inverter_proxy/plugins/__init__.py contains `from pv_inverter_proxy.plugins.sungrow import SungrowPlugin`
    - src/pv_inverter_proxy/plugins/__init__.py contains `rated_power=entry.rated_power` in the sungrow branch
    - src/pv_inverter_proxy/plugins/__init__.py contains `sungrow` in the ValueError message
    - `python -c "from pv_inverter_proxy.plugins import plugin_factory; from pv_inverter_proxy.config import InverterEntry; p = plugin_factory(InverterEntry(type='sungrow')); print(type(p).__name__)"` prints `SungrowPlugin`
    - `python -m pytest tests/ -x -q` exits with code 0
  </acceptance_criteria>
  <done>plugin_factory creates SungrowPlugin for type="sungrow" entries. All tests pass. Phase 38 backend is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config YAML -> plugin_factory | User-provided type string dispatches plugin creation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-06 | Tampering | entry.type value | mitigate | Factory uses explicit elif chain (not dynamic import). Unknown types raise ValueError. No arbitrary code execution possible from config. |
| T-38-07 | Denial of Service | Invalid host/port in config | accept | Same risk as all other plugins. ConnectionManager handles unreachable hosts with backoff. |
</threat_model>

<verification>
1. `python -c "from pv_inverter_proxy.plugins import plugin_factory; from pv_inverter_proxy.config import InverterEntry; p = plugin_factory(InverterEntry(type='sungrow', host='192.168.2.151', port=502)); print(type(p).__name__)"` prints `SungrowPlugin`
2. `python -m pytest tests/ -x -q` -- full suite green
3. `grep "sungrow" src/pv_inverter_proxy/plugins/__init__.py` shows factory branch exists
</verification>

<success_criteria>
- plugin_factory correctly dispatches type="sungrow" to SungrowPlugin
- SungrowPlugin receives host, port, unit_id, rated_power from InverterEntry
- ValueError message includes "sungrow" in the valid types list
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/38-plugin-core/38-02-SUMMARY.md`
</output>
